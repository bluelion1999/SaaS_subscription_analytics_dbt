name: 'saas_subscription_analytics'
version: '1.0.0'
config-version: 2

profile: 'saas_subscription_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  saas_subscription_analytics:
    +schema: raw

snapshots:
  saas_subscription_analytics:
    +schema: snapshots

models:
  saas_subscription_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
